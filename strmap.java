import java.util.*;
import java.util.Arrays;
import java.util.ArrayList;
import java.util.List;

class strmap{
    public static void main(String[] args) {
         List <Integer>number=Arrays.asList(20,22,34,23,43,22);
         System.out.println(number);//we can perform all operation but can not change remove and add
        // List<Double>result=number.stream().map(n->n-5.5).toList();
        // System.out.println(result);
        //filte with map
        // List<Integer>result=number.stream().filter(n->n<40).map(n->n+8).toList();
        // System.out.println(result);
         

        /// 1=>change in to upper case
        //  List <String>name=Arrays.asList("abhi","ravi","shivam");
        //  System.out.println(name);
        //  List<String>result=name.stream().map(n->n.toUpperCase()).toList();
        //if want to change then colletor 
        // System.out.println(result);

    


        //"item- ka use"
        // List <String>name=Arrays.asList("A","B","C");
        //   System.out.println(name);
        //   List<String>result=name.stream().map(n->("item-")+n).toList();
        //   System.out.println(result);


        


            






    }
}